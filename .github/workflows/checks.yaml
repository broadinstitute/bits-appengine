---
name: 'checks'

'on':
  pull_request:
    branches:
      - 'main'

jobs:
  linting:
    uses: broadinstitute/shared-workflows/.github/workflows/poetry-linting.yml@v2.0.0
    with:
      use_pylama: false
      use_ruff: true
