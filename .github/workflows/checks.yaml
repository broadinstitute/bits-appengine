---
name: checks

"on":
  pull_request:
    branches:
      - main
permissions:
  contents: read
  pull-requests: read

jobs:
  linting:
    uses: broadinstitute/shared-workflows/.github/workflows/python-lint.yaml@v6.3.0
