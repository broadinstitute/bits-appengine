---
name: deploy

"on":
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"
permissions:
  contents: read
  pull-requests: read

jobs:
  pypi_deploy:
    uses: broadinstitute/shared-workflows/.github/workflows/python-deploy-to-pypi.yaml@v6
    secrets:
      pypi_token: ${{ secrets.PYPI_TOKEN }}
